# PasswordGenerator
<table>
  <tr>
    <td width="45%">
      <img src="https://i.pinimg.com/736x/ee/17/ea/ee17ea454a90dd0b59157295eaabb1e4.jpg" width="100%" alt="Password Generator Logo">
    </td>
    <td width="55%" valign="top">
      <h2>Действительно то, что вам НУЖНО</h2>
      <p><strong>Генератор паролей</strong> с поддержкой консольного и графического интерфейса.</p>
      <h3>Основные возможности:</h3>
      <ul>
        <li>Поддержка латиницы, кириллицы, цифр и спецсимволов</li>
        <li>Криптографическая безопасность (SecureRandom)</li>
        <li>Пароли от 1 до 1 000 000 символов</li>
        <li>Тестирование производительности</li>
        <li>Два интерфейса: консоль и JavaFX GUI</li>
      </ul>
      <p><strong>Java 17</strong> или выше.</p>
    </td>
  </tr>
</table>

## 📦 Установка

### Сборка проекта
```bash
# Полная сборка
./gradlew build 
```
```bash
# Генерация документации
./gradlew javadoc 
```
```bash
# Тестирование (если не произошло при сборке)
./gradlew test 
```

### Структура выходных файлов
```
build/
└── docs/
    └── javadoc/
        └── index.html  <--  документация
```
```
build/
└── libs/
    └── PasswordGenerator.jar  <--  исполняемый файл
```
```
build/
└── repots/
    └── tests/
        └── test/
            └── index.html  <--  отчет по тестам
```

## 😍 Использование

### Запуск консольного интерфейса
```bash
# Способ 1: Через JAR файл
java -jar build/libs/PasswordGenerator.jar
```
```bash
# Способ 2: Через Gradle
./gradlew run --args="--ui=console"
```
```bash
# Способ 3: Без аргументов (консоль по умолчанию)
./gradlew run
```

### Запуск графического интерфейса
```bash
# Через JAR файл
java -jar build/libs/PasswordGenerator.jar --ui=gui
```
```bash
# Через Gradle
./gradlew run --args="--ui=gui"
```
